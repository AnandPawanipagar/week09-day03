'use strict'
var num=2;
console.log(typeof(num));
//There is no need to give data type in javascript since it is a dynamically typed language which means it will automatically determine the data type.